# Portfolio access credentials.
# Copy to .env.local and fill in. Both must be set for auth to activate.
# If either is blank, the site is accessible without login (useful for local dev).
#
# 注意: クライアントサイド認証なので、ビルド成果物に値が露出します。
# 本気で秘匿したい情報には使わないこと。
VITE_AUTH_ID=
VITE_AUTH_PASSWORD=
